# Frontend Application Directory
# This directory will contain the client-side application code or Temporal Client implementation.

## Proposed Structure
- `client/`: Temporal Client code (Go/TypeScript/Java)
- `ui/`: Custom UI components (if extending Temporal Web)
- `workers/`: Workflow and Activity implementations (often part of backend, but can be here)

*Coming soon.*
